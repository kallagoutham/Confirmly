[tool.black]
line-length = 120
target-version = ["py313"]
exclude = '''
/(
  \.git
  | \.venv
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
line_length = 120
skip = [".git", ".venv", "build", "dist"]
